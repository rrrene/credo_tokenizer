# CredoTokenizer

This is a fork of Elixir's tokenizer, since the tokenizer is a private API of the Elixir compiler.

For the time being, this should be considered an experimental private API of Credo.
